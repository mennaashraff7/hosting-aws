{
    "scripts": {
        "frontend:install": "cd udagram-frontend && npm install --legacy-peer-deps",
        "backend:install": "cd udagram-api && npm install --legacy-peer-deps",
        "frontend:build": "cd udagram-frontend && export SET NODE_OPTIONS=--openssl-legacy-provider && npm run build",
        "backend:build": "cd udagram-api && npm run build",
        "frontend:deploy": "cd udagram-frontend && npm run deploy",
        "backend:aws-eb": "cd udagram-api && npm run aws-eb",
        "backend:deploy": "cd udagram-api && npm run deploy",
        "frontend:test": "cd udagram-frontend && export SET NODE_OPTIONS=--openssl-legacy-provider && npm run test",
        "frontend:e2e": "cd udagram-frontend && npm run e2e",
        "frontend:karmaCli": "cd udagram-frontend && export SET NODE_OPTIONS=--openssl-legacy-provider && npm run karmaCli"
    },
    "devDependencies": {
        "karma": "^6.3.19",
        "karma-chrome-launcher": "^3.1.1",
        "karma-coverage-istanbul-reporter": "^3.0.3",
        "karma-jasmine": "^5.0.0",
        "karma-jasmine-html-reporter": "^1.7.0"
    }
}
